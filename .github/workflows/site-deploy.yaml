name: "Deploy to GitHub Pages"

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docs:
    # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
    permissions:
      contents: read
      pages: write
      id-token: write

    uses: letsbuildawiki/.github/.github/workflows/site-from-template.yaml@5c8e726115d6584ef0344b1d5447f536a7a4493a # v1.0.0
